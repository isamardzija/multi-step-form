import React from "react";
import ReactDOM from "react-dom/client";
import { LifeCasco } from "./LifeCasco";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LifeCasco />
  </React.StrictMode>
);
